//SPDX-License-Identifier: Unlicense
pragma solidity ^0.8.0;

import "./SafeMath.sol";

contract Project {
    using SafeMath for uint256;

    // Data struct
    enum State {
        Fundraising,
        Expired,
        Successful
    }

    // state variables
}
